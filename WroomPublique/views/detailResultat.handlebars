<center>
    <h1>Liste des résultats</h1>
    <table id="tableMenu">
        <tr>
            <th>Place</th>
            <th>Pilote</th>
            <th>Temps</th>
            <th>Points</th>
        </tr>
        {{#each tableau}}
            <tr>
                <td class="ligne">{{inc @index}}</td>
                <td class="ligne">{{this.1.PILNOM}}</td>
                <td class="ligne">{{this.1.TEMPSCOURSE}}</td>
                <td class="ligne">{{#if this.0.PTNBPOINTSPLACE}}
                     {{this.0.PTNBPOINTSPLACE}}
                     {{else}}
                     0
                     {{/if}}
                </td>
            </tr>
        {{/each}}
    </table>
</center>
<br class="myboth" />
