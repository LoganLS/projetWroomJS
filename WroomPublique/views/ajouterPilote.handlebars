<center><h1>Ajouter un Pilote</h1></center>

<form method="post">
    <table>
        
        <tr><td>Prénom</td><td><input type="text" name="prenom" required/></td></tr>
        <tr><td>Nom</td><td><input type="text" name="nom" required/></td></tr>
        <tr><td>Date de naissance</td>
            <td>
                <input type="text" width="50" name="jour" size="2" required/> / 
                <input type="text" name="mois" size="2" required/> / 
                <input type="text" name="annee" size="4" required/>
            </td>
        </tr>
        <tr>
            <td>Nationalité</td>
            <td>
                <select name="nationalite">
                    {{#each listeNationalite}}
                        <option name="{{this.paynum}}">{{this.paynat}}</option>
                    {{/each}}
                </select>
            </td>
        </tr>
        <tr>
            <td>Ecurie </td><td>
            <select name="ecurie">
                {{#each listeEcurie}}
                    <option name="{{this.ecunum}}">{{this.ecunom}}</option>
                {{/each}}
            </select>
            </td>
        </tr>
        <tr><td>Points</td><td><input type="text" name="points" required/> pts</td></tr>
        <tr><td>Poids</td><td><input type="text" name="poids" required/> kg</td></tr>
        <tr><td>Taille</td><td><input type="text" name="taille" required/> cm</td></tr>
        <tr><td>Description</td><td><textarea name="description" cols="75" rows="5" required></textarea></td></tr>
        
    </table>
        
    <br/><br/>
    <input href="/menuPilotes/ajouterPilote/ajout" type="submit" name="ajouter" value="Ajouter" id="bouton"/>
</form>

<br class="myboth" />